{
  "name": "calendar-sync-backend",
  "version": "1.0.0",
  "description": "Backend service for Calendar Sync - extracts calendar events from emails using Claude AI",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "googleapis": "^144.0.0",
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vercel": "^37.13.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
